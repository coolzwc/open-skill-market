# GitHub Personal Access Tokens
# Create tokens at: https://github.com/settings/tokens
# Required scopes: public_repo (for searching public repositories)
#
# Each token from a DIFFERENT GitHub account adds 5000 req/hour capacity
# (Same account tokens share rate limit)

# Primary token (required)
GITHUB_TOKEN=your_github_token_here

# Additional tokens from different accounts (optional, 1-5)
# Using EXTRA_TOKEN_ prefix because GitHub Actions reserves GITHUB_ prefix
# Stops at first empty token
# EXTRA_TOKEN_1=second_account_token
# EXTRA_TOKEN_2=third_account_token
# EXTRA_TOKEN_3=fourth_account_token
# EXTRA_TOKEN_4=fifth_account_token
# EXTRA_TOKEN_5=sixth_account_token

# =====================================
# Crawler Options
# =====================================

# Zip package generation (default: true)
# Set to "false" to disable zip package generation
# GENERATE_ZIPS=false

# Custom base URL for zip packages (optional)
# Default: https://raw.githubusercontent.com/coolzwc/open-skill-market/main/market/zips
# ZIP_BASE_URL=https://your-cdn.com/zips

# =====================================
# Cache Options
# =====================================

# Cache migration strategy when version mismatch (default: migrate)
# Options:
#   - "migrate": Try to migrate old cache data to new format
#   - "discard": Discard old cache and start fresh
# CACHE_MIGRATION_STRATEGY=migrate
